name = "pulse-data-worker"
main = "index.js"
compatibility_date = "2024-12-01"

[vars]
DATA_URL = "https://wilfgrainger.github.io/gov-metrics/daily_threat_data.json"

# Cron trigger: refresh data every 4 hours (mirrors GitHub Actions schedule)
# [triggers]
# crons = ["0 */4 * * *"]
